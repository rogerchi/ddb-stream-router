# Workflow Guidelines

## After Task Completion

- Commit changes after each task with a concise message starting with:
  - `feat:` for new features
  - `chore:` for maintenance tasks, checkpoints, or non-functional changes
  - `fix:` for bug fixes
